# ============================================
# 操作系统原理可视化实验展示平台 - 依赖清单
# ============================================
#
# Python 版本要求: Python 3.8 - 3.11 (推荐 3.9 或 3.10)
#
# 注意事项:
# 1. Python 3.12+ 可能存在兼容性问题，建议使用 3.9-3.11
# 2. 建议使用虚拟环境安装依赖
# 3. Windows系统推荐使用 pip 安装
#
# 安装命令:
#   pip install -r requirements.txt
#
# 创建虚拟环境(可选):
#   python -m venv venv
#   venv\Scripts\activate  (Windows)
#   source venv/bin/activate  (Linux/Mac)
#
# ============================================

# 核心GUI框架 (必需)
# PyQt5 是本项目的核心依赖，用于构建图形界面
# 版本说明:
#   - 5.15.x: 稳定版本，推荐使用
#   - 最低要求: 5.15.0
#   - 最高测试: 5.15.10
PyQt5>=5.15.0,<6.0.0

# PyQt5 的 Qt 绑定 (通常随 PyQt5 自动安装)
PyQt5-Qt5>=5.15.0,<6.0.0

# PyQt5 的 sip 模块 (通常随 PyQt5 自动安装)
PyQt5-sip>=12.8.0

# 系统监控库 (用于任务管理器模块)
# 用于获取系统CPU、内存、进程等信息
# 注意: 如果安装失败，任务管理器模块将使用模拟数据
psutil>=5.9.0
